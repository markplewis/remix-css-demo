{
  "private": true,
  "sideEffects": false,
  "scriptsComments": {
    "build": "Builds app for production. Sets process.env.NODE_ENV to production and minifies output for deployment.",
    "dev": "Watches files and builds app for development when changes are made. Starts Remix app server in development mode.",
    "check": "Check for outdated, incorrect, and unused NPM module dependencies."
  },
  "scripts": {
    "build": "remix build",
    "dev": "remix dev",
    "check": "npx npm-check -s -u"
  },
  "dependencies": {
    "@remix-run/node": "^1.13.0",
    "@remix-run/react": "^1.13.0",
    "@remix-run/vercel": "^1.13.0",
    "@vercel/node": "^2.9.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-use": "^17.4.0"
  },
  "devDependencies": {
    "@remix-run/dev": "^1.13.0",
    "@remix-run/eslint-config": "^1.13.0",
    "@remix-run/serve": "^1.13.0",
    "eslint": "^8.34.0",
    "eslint-config-prettier": "^8.6.0",
    "eslint-import-resolver-alias": "^1.1.2",
    "eslint-plugin-import": "^2.27.5",
    "prettier": "^2.8.4"
  }
}
